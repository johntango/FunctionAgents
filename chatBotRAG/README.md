# Responses RAG Chatbot using RAG

The goal is to use a RAG system to enhance query answers

1. The RAG (vectorDB) can be retrieved by "NAME" and files added to it
2. A new VectorDB can be made with files being added to it. Files will be loaded
   from folder presently named RAGData

## The query is entered separately and answered from the VectorDB loaded.
